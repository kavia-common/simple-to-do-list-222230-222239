# simple-to-do-list-222230-222239

This workspace contains the frontend-only React to-do app. It stores data in the browser (localStorage) and does not require backend services.

Environment variables:
- Only REACT_APP_LOG_LEVEL is optionally used by the frontend to enable debug logs in development when set to `debug`. All other listed REACT_APP_* variables are unused by this frontend-only app.